# ── 必填（生产）────────────────────────────────────────────────
# 会话签名密钥（lib/session.ts 用它做 HMAC-SHA256 签名）。
# 不设则回退到公开默认值 → 会话可被任意伪造，能冒充任意用户/管理员！
# 生成强随机值：openssl rand -base64 32
NEXTAUTH_SECRET=

# provider API Key 的静态加密密钥（lib/crypto.ts，AES-256-GCM）。
# 不设则回退到 NEXTAUTH_SECRET / 公开默认值。⚠️ 一旦用它加密过 key，就别再改——
# 改了会导致后台已存的服务商 API Key 解不开（AI 调不通，需重新填 key）。
# 生成：openssl rand -base64 32
ENCRYPTION_KEY=

# ── 数据库 ──────────────────────────────────────────────────────
# 注意：数据库连接写死在 prisma/schema.prisma（当前 SQLite: file:./dev.db），
# 代码并不读取 DATABASE_URL。生产请确保该 .db 文件在持久化、可备份的卷上，并开启 WAL（见 DEPLOY.md）。
# 若要换 Postgres，需改 schema.prisma 的 datasource 后再启用 DATABASE_URL。
# DATABASE_URL=

# 生成图片落盘目录（数据库只存站内路径，图片走 /api/uploads 服务）。
# 默认 cwd 下 data/uploads；生产请指向持久化、可备份的卷。
# UPLOAD_DIR=/data/uploads

# ── AI 上游（可选）──────────────────────────────────────────────
# 推荐在后台「AI 服务商」里配置；下面的环境变量仅作「未配置任何服务商时」的兜底默认提供商。
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1

# ── 运行环境 ────────────────────────────────────────────────────
# 生产由 `next start` 自动设为 production；本地开发无需设置。
# NODE_ENV=production
